"""
所有 Prompt 集中管理
"""

# ── 意图分类 ─────────────────────────────────────────────
INTENT_CLASSIFY_PROMPT = """你是一个意图分类器。将用户输入分为以下3类，只输出对应的英文标签：

- knowledge: 需要查询已有知识库/论文内容才能回答的问题（如：Transformer的自注意力机制是什么？RAG有哪些组件？这篇论文的方法是什么？）
- task: 需要调用外部工具执行操作（如：搜索最新论文、查找某作者的论文、生成引用格式、翻译这段文字、总结这篇论文）
- chitchat: 闲聊、问候、感谢、无实际信息需求

示例：
用户: "Attention机制是怎么工作的？" → knowledge
用户: "帮我搜索关于RAG的最新论文" → task
用户: "你好！" → chitchat
用户: "BERT和GPT有什么区别？" → knowledge
用户: "查一下Yoshua Bengio的论文" → task
用户: "谢谢" → chitchat
用户: "把这段摘要翻译成中文" → task
用户: "论文里提到的实验结果是什么？" → knowledge

用户输入："{query}"
分类结果："""

# ── Query 改写（指代消解）──────────────────────────────────
QUERY_REWRITE_PROMPT = """你是一个对话理解助手。请根据对话历史，将用户的最新问题改写成完整、独立的问题。
主要任务：消解指代词（"它"、"这个"、"那篇论文"等），补全省略的主语。
如果最新问题已经完整清晰，直接原样返回。
只输出改写后的问题，不要解释。

对话历史：
{history}

最新问题：{query}

改写后的完整问题："""

# ── CRAG 文档质量评分 ────────────────────────────────────
CRAG_GRADING_PROMPT = """你是一个文档相关性评估专家。判断以下检索到的文档片段是否能帮助回答用户的问题。

用户问题：{query}

文档片段：
{document}

请评估相关性，只输出以下三个标签之一：
- relevant: 文档包含回答问题所需的关键信息
- ambiguous: 文档部分相关但信息不够完整
- irrelevant: 文档与问题无关

评估结果："""

# ── RAG 答案生成 ─────────────────────────────────────────
RAG_GENERATE_PROMPT = """你是一个AI学术研究助手。请严格根据以下检索到的论文片段来回答用户的问题。

规则：
1. 只使用提供的上下文中的信息
2. 如果上下文信息不足以回答，明确告知用户
3. 引用具体来源（如"根据论文XXX中提到..."）
4. 回答要准确、专业、有条理

检索到的论文片段：
{context}

用户问题：{query}

回答："""

# ── 闲聊回复 ─────────────────────────────────────────────
CHITCHAT_PROMPT = """你是一个友好的AI学术研究助手。用户在进行闲聊，请简短、友好地回复。
你的特长是帮助用户搜索论文、回答学术问题、管理阅读笔记。

用户：{query}
回复："""

# ── Agent 工具规划 ───────────────────────────────────────
TOOL_PLANNING_PROMPT = """你是一个研究助手。请根据用户的问题，制定一个工具调用计划。

用户问题：{query}

可用工具：
{tools_description}

请制定计划，输出 JSON 数组格式：
[
  {{"tool": "工具名", "args": {{"参数名": "值"}}, "purpose": "这一步的目的"}}
]

注意：
- 只使用可用工具列表中的工具
- 计划步骤不超过3步
- 每步要有明确的目的

工具调用计划："""

# ── CRAG Query 重写（ambiguous 时改写检索词）───────────────
CRAG_QUERY_REWRITE_PROMPT = """检索到的文档与用户问题部分相关但不够完整。请基于原始问题和已检索到的部分信息，重新生成一个更精确的搜索query。

原始问题：{query}
已检索到的部分信息摘要：{partial_context}

要求：
- 换一种表述方式来描述同一个问题
- 可以更具体、更聚焦，以便检索到更相关的文档
- 只输出重写后的query，不要解释

重写后的query："""

# ── Agent 结果综合 ───────────────────────────────────────
TOOL_SYNTHESIS_PROMPT = """基于以下研究步骤和结果，综合回答用户的问题。

用户问题：{query}

研究过程：
{tool_history}

请给出结构化的回答，包含关键发现和信息来源。

综合回答："""

# ── Faithfulness 评估（LLM-as-Judge）─────────────────────
FAITHFULNESS_JUDGE_PROMPT = """请判断以下回答中的信息是否都能在给定上下文中找到依据。

上下文：
{context}

回答：
{answer}

对回答中的每个关键声明，判断是否有据(supported)或无据(unsupported)。
输出JSON格式：{{"score": 0到1的小数, "unsupported_claims": ["无据声明1", ...]}}

评估结果："""

# ── Answer Relevancy 评估 ────────────────────────────────
RELEVANCY_JUDGE_PROMPT = """请判断以下回答是否准确回答了用户的问题。

用户问题：{query}
回答：{answer}

评分标准：
- 1.0: 完全回答了问题
- 0.7: 大部分回答了但有遗漏
- 0.4: 部分相关但偏离重点
- 0.1: 基本没有回答问题

只输出一个0到1的小数分数："""
